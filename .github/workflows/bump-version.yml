name: Bump version

on:
  push:
    branches: [main]
    paths: ['src/**', 'container/**']

jobs:
  bump-version:
    runs-on: ubuntu-latest
    steps:
      - name: Check GitHub App secrets
        id: app-secrets
        env:
          APP_ID: ${{ secrets.APP_ID }}
          APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
        run: |
          if [ -n "$APP_ID" ] && [ -n "$APP_PRIVATE_KEY" ]; then
            echo "has_app_secrets=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_app_secrets=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/create-github-app-token@v1
        if: steps.app-secrets.outputs.has_app_secrets == 'true'
        id: app-token
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: actions/checkout@v4
        if: steps.app-secrets.outputs.has_app_secrets == 'true'
        with:
          token: ${{ steps.app-token.outputs.token }}

      - name: Bump patch version
        if: steps.app-secrets.outputs.has_app_secrets == 'true'
        run: |
          npm version patch --no-git-tag-version
          git add package.json package-lock.json
          git diff --cached --quiet && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          VERSION=$(node -p "require('./package.json').version")
          git commit -m "chore: bump version to $VERSION"
          git pull --rebase
          git push

      - name: Skip when GitHub App secrets are missing
        if: steps.app-secrets.outputs.has_app_secrets != 'true'
        run: echo "Skipping bump-version because APP_ID or APP_PRIVATE_KEY is not configured."
